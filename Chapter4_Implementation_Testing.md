# الفصل الرابع: تطبيق النظام واختباره
# Chapter 4: System Implementation and Testing

---

## 4.1 المقدمة (Introduction)

تم بناء نظام **AutoGo** على **معمارية ثلاثية الطبقات (Three-Tier Architecture)** لضمان الفصل بين المكونات وسهولة الصيانة والتطوير. يوضح الجدول التالي المكونات الرئيسية والتقنيات المستخدمة:

| المكون (Component) | التقنية المستخدمة (Technology) | الوصف |
|---|---|---|
| تطبيق الموبايل (Mobile App) | React Native + Expo | واجهة المستخدم للعميل - تعمل على Android و iOS |
| الخادم الخلفي (Backend Server) | Node.js + Express.js | معالجة الطلبات وإدارة البيانات والمنطق التجاري |
| قاعدة البيانات (Database) | PostgreSQL + Prisma ORM | تخزين واسترجاع البيانات بشكل منظم |
| التواصل الفوري (Real-time) | Socket.IO + Redis | البث المباشر لتتبع الموقع والمحادثات |
| المصادقة (Authentication) | Clerk + JWT | إدارة الهوية وتسجيل الدخول الآمن |

### لماذا معمارية ثلاثية الطبقات؟
- **الاستقلالية**: كل طبقة تعمل بشكل مستقل، مما يسمح بتطوير أي جزء دون التأثير على الأجزاء الأخرى.
- **قابلية التوسع**: يمكن توسيع أي طبقة بشكل منفصل حسب الحاجة.
- **سهولة الصيانة**: عزل المشاكل وإصلاحها يصبح أسهل وأسرع.
- **إعادة الاستخدام**: الـ API يمكن استخدامه من أي عميل آخر (تطبيق ويب، لوحة إدارة).

---

## 4.2 تطبيق الموبايل - React Native (Frontend)

### 4.2.1 لماذا React Native و Expo؟

| الميزة | الشرح |
|---|---|
| **Cross-Platform** | كود واحد يعمل على Android و iOS مما يوفر وقت التطوير |
| **Expo Framework** | يوفر أدوات جاهزة للكاميرا والموقع والإشعارات والتخزين الآمن |
| **Hot Reload** | المطور يشوف التغييرات فورياً أثناء التطوير |
| **مجتمع كبير** | مكتبات وحزم متاحة تُسرّع التطوير بشكل كبير |
| **أداء عالي** | أداء قريب من التطبيقات الأصلية (Native) |

### 4.2.2 النمط المعماري: Modular Architecture

تم تبني **المعمارية القائمة على الوحدات (Modular Architecture)** مع نمط **Redux** لإدارة الحالة المركزية. هذا النمط يفصل بين:
- **الشاشات (Screens)**: عرض الواجهات للمستخدم
- **المكونات (Components)**: عناصر قابلة لإعادة الاستخدام
- **المتجر (Store/Redux)**: إدارة الحالة المركزية للتطبيق
- **الخدمات (Services/API)**: التواصل مع الخادم الخلفي

### 4.2.3 هيكل المجلدات (Folder Structure)

| المجلد | الوظيفة | الملفات الرئيسية |
|---|---|---|
| `src/screens/` | شاشات التطبيق (41 شاشة) | HomeScreen, LoginScreen, SOSScreen, TrackingScreen... |
| `src/components/` | مكونات مشتركة قابلة لإعادة الاستخدام | Button, Card, Header, Input, MapPlaceholder, StarRating |
| `src/store/slices/` | شرائح Redux لإدارة الحالة | authSlice, garageSlice, ordersSlice, servicesSlice, addressSlice |
| `src/navigation/` | إعدادات التنقل بين الشاشات | AppNavigator.tsx (Stack + Bottom Tabs) |
| `src/api/` | عميل HTTP للتواصل مع الباك إند | apiClient.ts |
| `src/theme/` | نظام التصميم (الألوان، الخطوط، المسافات) | colors.ts, typography.ts, spacing.ts |
| `src/data/` | بيانات تجريبية للتطوير | mockData.ts |
| `src/types.ts` | تعريفات TypeScript للأنواع | User, Car, Order, Workshop, Driver... |
| `App.tsx` | نقطة البداية الرئيسية | ClerkProvider + Redux Provider |

### 4.2.4 إدارة الحالة - Redux Toolkit

تم استخدام **Redux Toolkit** كحل مركزي لإدارة حالة التطبيق. يتكون المتجر من 5 شرائح (Slices):

| الشريحة (Slice) | الوظيفة | البيانات المُدارة |
|---|---|---|
| `authSlice` | إدارة المصادقة والمستخدم | بيانات المستخدم، التوكن، حالة تسجيل الدخول |
| `garageSlice` | إدارة المرآب والسيارات | قائمة السيارات، السيارة النشطة |
| `ordersSlice` | إدارة الطلبات والتتبع | الطلبات النشطة، السجل، بيانات التتبع |
| `servicesSlice` | إدارة الخدمات والورش | الخدمات المتاحة، الورش، الفلاتر |
| `addressSlice` | إدارة العناوين المحفوظة | قائمة العناوين، العنوان الافتراضي |

### 4.2.5 نظام التنقل (Navigation)

تم استخدام **React Navigation v7** مع نمطين:
- **Stack Navigator**: للتنقل الهرمي بين الشاشات (46 شاشة)
- **Bottom Tab Navigator**: للتنقل الرئيسي (4 تبويبات: الرئيسية، جراجي، طلباتي، حسابي)

يتم التحكم في التنقل بناءً على حالة المصادقة:
- **غير مُسجّل**: يرى شاشات Splash → Onboarding → Login → OTP
- **مُسجّل**: يرى الشاشة الرئيسية مع كل الوظائف

---

### 4.2.6 الشاشات الرئيسية المبنية

#### 🔐 شاشة تسجيل الدخول (Login Screen)
**الوظيفة:** البوابة الأمنية للنظام
- تسجيل الدخول عبر **Google OAuth** باستخدام Clerk
- تسجيل الدخول عبر **Apple OAuth** (على أجهزة iOS)
- واجهة بتدرج لوني داكن (Dark Gradient) مع شعار التطبيق
- رابط الشروط والأحكام وسياسة الخصوصية
- استخدام `expo-web-browser` لفتح نوافذ OAuth بشكل آمن

**لماذا مهمة؟** توفر مصادقة آمنة عبر مزودي هوية موثوقين (Google/Apple) بدون الحاجة لكلمات مرور تقليدية.

<!-- ضع هنا اسكرينشوت شاشة تسجيل الدخول -->
<!-- شكل (4.1): شاشة تسجيل الدخول - Login Screen -->
<!-- الوصف: تعرض الشاشة شعار AutoGo في الأعلى مع تدرج لوني داكن، وزر "المتابعة باستخدام جوجل" باللون الأبيض، وزر "المتابعة باستخدام أبل" باللون الأسود، ورابط الشروط والأحكام في الأسفل -->

---

#### 🏠 الشاشة الرئيسية (Home Screen)
**الوظيفة:** لوحة تحكم المستخدم الرئيسية
- **ترحيب شخصي**: عرض اسم المستخدم وصورته الشخصية
- **عرض السيارات**: كاروسيل أفقي لعرض سيارات المستخدم مع بياناتها (الماركة، الموديل، رقم اللوحة، عداد الكيلومترات)
- **تنبيهات السيارات**: عرض تنبيهات الصيانة والتأمين بشكل بارز
- **زر الطوارئ SOS**: زر متحرك (Animated Pulse) بتدرج أحمر لطلب ونش فوري
- **شبكة الخدمات**: 6 خدمات ذكية (صيانة دورية، غسيل، فحص بطارية، إطارات، كهرباء، فحص شامل)
- **الطلبات النشطة**: عرض أحدث طلبين نشطين مع حالتهم

<!-- ضع هنا اسكرينشوت الشاشة الرئيسية -->
<!-- شكل (4.2): الشاشة الرئيسية - Home Screen -->
<!-- الوصف: تعرض الشاشة ترحيب بالمستخدم مع صورته، كاروسيل السيارات المسجلة، زر SOS الأحمر المتحرك، شبكة الخدمات الذكية (6 أيقونات)، والطلبات النشطة -->

---

#### 🚨 شاشة الطوارئ (SOS Screen)
**الوظيفة:** طلب خدمة ونش إنقاذ فوري
- **خريطة تفاعلية**: لتحديد موقع العطل بدقة عبر GPS
- **تحديد الموقع التلقائي**: باستخدام `expo-location`
- **عرض السيارة النشطة**: عرض بيانات السيارة المختارة
- **زر الطوارئ**: بتدرج لوني أحمر مع تأثيرات بصرية

**المسار الكامل لطلب الونش:**
SOS → اختيار نوع الونش (TowType) → الدفع (Payment) → البحث عن سائق (Searching) → التتبع المباشر (Tracking)

<!-- ضع هنا اسكرينشوت شاشة الطوارئ -->
<!-- شكل (4.3): شاشة طلب الونش - SOS Screen -->
<!-- الوصف: تعرض الشاشة خريطة لتحديد موقع العطل، بطاقة الموقع الحالي، بيانات السيارة النشطة، وزر "اطلب ونش الآن" الأحمر الكبير -->

<!-- ضع هنا اسكرينشوت شاشة اختيار نوع الونش -->
<!-- شكل (4.4): شاشة اختيار نوع الونش - Tow Type Screen -->
<!-- الوصف: تعرض أنواع الونش المتاحة (عادي، هيدروليك، ثقيل) مع الأسعار والأيقونات -->

<!-- ضع هنا اسكرينشوت شاشة البحث عن سائق -->
<!-- شكل (4.5): شاشة البحث عن سائق - Searching Screen -->
<!-- الوصف: تعرض أنيميشن بحث مع رسالة "جاري البحث عن أقرب ونش" -->

---

#### 📍 شاشة التتبع المباشر (Tracking Screen)
**الوظيفة:** تتبع الونش/السائق في الوقت الفعلي
- **خريطة تفاعلية حقيقية**: باستخدام `react-native-maps` مع ستايل داكن مخصص
- **خوارزمية Haversine**: لحساب المسافة بين المستخدم وأقرب ورشة
- **محاكاة حركة السائق**: السائق يتحرك تدريجياً نحو موقع المستخدم
- **مسار الطريق**: رسم مسار الوصول باستخدام OpenRouteService API
- **بيانات السائق**: الاسم، التقييم، نوع الونش، لوحة المركبة
- **أزرار التواصل**: محادثة (Chat) واتصال مباشر (Call)
- **شريط الحالة الزمني (Timeline)**: تتبع مراحل الطلب خطوة بخطوة
- **شارة ETA**: الوقت المتوقع للوصول والمسافة المتبقية
- **بث مباشر**: شارة "بث مباشر" تؤكد أن التتبع لحظي

<!-- ضع هنا اسكرينشوت شاشة التتبع المباشر -->
<!-- شكل (4.6): شاشة التتبع المباشر - Tracking Screen -->
<!-- الوصف: تعرض خريطة تفاعلية داكنة مع موقع المستخدم (دبوس أزرق) وموقع الونش (دبوس أحمر) ومسار الطريق، شارة ETA، بيانات السائق، أزرار المحادثة والاتصال، وشريط حالة الطلب الزمني -->

---

#### 🔧 شاشة الخدمات والصيانة (Services & Booking Flow)
**الوظيفة:** تصفح وحجز خدمات الصيانة
- **شاشة الخدمات (Services)**: عرض جميع الخدمات المتاحة مصنفة بفئات
- **قائمة الورش (WorkshopList)**: عرض الورش المتاحة مع التقييم والموقع
- **تفاصيل الورشة (WorkshopDetail)**: معلومات تفصيلية، ساعات العمل، التقييمات، الخدمات المقدمة
- **اختيار طريقة الخدمة (ServiceMethod)**: في المركز أو خدمة متنقلة
- **حجز الموعد (Booking)**: اختيار التاريخ والوقت المناسب
- **مراجعة الحجز (BookingReview)**: تأكيد كل التفاصيل قبل الإرسال

<!-- ضع هنا اسكرينشوت شاشة الخدمات -->
<!-- شكل (4.7): شاشة الخدمات المتاحة - Services Screen -->
<!-- الوصف: تعرض قائمة الخدمات مصنفة بفئات مع أيقونات وأسعار -->

<!-- ضع هنا اسكرينشوت شاشة قائمة الورش -->
<!-- شكل (4.8): شاشة قائمة الورش - Workshop List Screen -->
<!-- الوصف: تعرض بطاقات الورش المتاحة مع التقييم والعنوان وحالة الفتح -->

<!-- ضع هنا اسكرينشوت شاشة تفاصيل الورشة -->
<!-- شكل (4.9): شاشة تفاصيل الورشة - Workshop Detail Screen -->
<!-- الوصف: تعرض صورة الورشة، التقييم، ساعات العمل، الخدمات المقدمة، وزر الحجز -->

<!-- ضع هنا اسكرينشوت شاشة حجز الموعد -->
<!-- شكل (4.10): شاشة حجز الموعد - Booking Screen -->
<!-- الوصف: تعرض اختيار التاريخ والوقت المناسب مع تفاصيل الخدمة المختارة -->

---

#### 🚗 شاشات إدارة المرآب (Garage Screens)
**الوظيفة:** إدارة سيارات المستخدم
- **المرآب (Garage)**: عرض جميع السيارات المسجلة
- **إضافة سيارة (AddCar)**: إدخال بيانات السيارة (الماركة، الموديل، السنة، اللوحة، اللون، عداد الكيلومترات)
- **تفاصيل السيارة (CarDetails)**: عرض كامل مع التنبيهات والتاريخ

<!-- ضع هنا اسكرينشوت شاشة المرآب -->
<!-- شكل (4.11): شاشة المرآب - Garage Screen -->
<!-- الوصف: تعرض جميع السيارات المسجلة في بطاقات مع بيانات كل سيارة وزر إضافة سيارة جديدة -->

<!-- ضع هنا اسكرينشوت شاشة إضافة سيارة -->
<!-- شكل (4.12): شاشة إضافة سيارة جديدة - Add Car Screen -->
<!-- الوصف: تعرض نموذج إدخال بيانات السيارة (الماركة، الموديل، السنة، اللوحة، اللون) -->

<!-- ضع هنا اسكرينشوت شاشة تفاصيل السيارة -->
<!-- شكل (4.13): شاشة تفاصيل السيارة - Car Details Screen -->
<!-- الوصف: تعرض بيانات السيارة الكاملة مع التنبيهات والصيانات السابقة -->

---

#### 💬 شاشات التواصل (Communication Screens)
- **المحادثة (Chat)**: تراسل فوري مع السائق أثناء الطلب
- **الاتصال (Call)**: شاشة اتصال مباشر بالسائق
- **التقييم (Rating)**: تقييم الخدمة بالنجوم مع تعليق

<!-- ضع هنا اسكرينشوت شاشة المحادثة -->
<!-- شكل (4.14): شاشة المحادثة مع السائق - Chat Screen -->
<!-- الوصف: تعرض محادثة فورية بين المستخدم والسائق مع فقاعات رسائل -->

<!-- ضع هنا اسكرينشوت شاشة التقييم -->
<!-- شكل (4.15): شاشة تقييم الخدمة - Rating Screen -->
<!-- الوصف: تعرض نجوم التقييم وحقل التعليق وأزرار الوسوم (سريع، محترف، ممتاز) -->

---

#### 👤 شاشات الملف الشخصي والإعدادات
| الشاشة | الوظيفة |
|---|---|
| Profile | عرض بيانات المستخدم والعضوية والنقاط |
| EditProfile | تعديل البيانات الشخصية |
| Addresses | إدارة العناوين المحفوظة |
| Wallet | المحفظة الإلكترونية والرصيد (EGP) |
| PaymentMethods | طرق الدفع (Meeza, Vodafone Cash, Fawry, Visa) |
| History | سجل الطلبات السابقة |
| Invoice | الفاتورة الإلكترونية الرقمية |
| Notifications | الإشعارات والتنبيهات |
| Settings | إعدادات التطبيق |
| HelpCenter | مركز المساعدة والأسئلة الشائعة |
| Support | الدعم الفني |
| Terms | الشروط والأحكام |
| PrivacyPolicy | سياسة الخصوصية |

<!-- ضع هنا اسكرينشوت شاشة الملف الشخصي -->
<!-- شكل (4.16): شاشة الملف الشخصي - Profile Screen -->
<!-- الوصف: تعرض صورة المستخدم، الاسم، نوع العضوية، النقاط، وقائمة الإعدادات والخيارات -->

<!-- ضع هنا اسكرينشوت شاشة المحفظة -->
<!-- شكل (4.17): شاشة المحفظة الإلكترونية - Wallet Screen -->
<!-- الوصف: تعرض رصيد المحفظة بالجنيه المصري (EGP) وسجل المعاملات الأخيرة -->

<!-- ضع هنا اسكرينشوت شاشة الإشعارات -->
<!-- شكل (4.18): شاشة الإشعارات - Notifications Screen -->
<!-- الوصف: تعرض قائمة الإشعارات مصنفة (عاجل، نجاح، عروض، تذكير) مع التوقيت -->

<!-- ضع هنا اسكرينشوت شاشة الإعدادات -->
<!-- شكل (4.19): شاشة الإعدادات - Settings Screen -->
<!-- الوصف: تعرض خيارات التطبيق مثل اللغة، الوضع الداكن، الإشعارات، ومعلومات الحساب -->

<!-- ضع هنا اسكرينشوت شاشة سجل الطلبات -->
<!-- شكل (4.20): شاشة سجل الطلبات السابقة - History Screen -->
<!-- الوصف: تعرض قائمة بجميع الطلبات المكتملة مع التاريخ والسعر والحالة -->

---

### 4.2.7 نظام التصميم (Design System)

تم بناء نظام تصميم متكامل يضمن اتساق الواجهات:

**لوحة الألوان الأساسية:**
| اللون | الكود | الاستخدام |
|---|---|---|
| خلفية رئيسية | `#0A1F2E` | الخلفية الأساسية (Dark Navy) |
| لون التمييز | `#2DD4BF` | الأزرار والعناصر التفاعلية (Teal/Cyan) |
| لون الطوارئ | `#E53E3E` | زر SOS والتنبيهات العاجلة |
| نص رئيسي | `#FFFFFF` | النصوص الأساسية |
| نص ثانوي | `rgba(255,255,255,0.7)` | النصوص الفرعية |

**المكونات المشتركة (Reusable Components):**
- **Button**: 5 أنماط (primary, secondary, danger, outline, ghost) و3 أحجام
- **Card**: 6 أنماط (glass, solid, white, accent, danger, warning)
- **Input**: حقل إدخال مع أيقونات وتحقق من الأخطاء
- **Header**: شريط علوي مع أزرار رجوع وإجراءات
- **StarRating**: تقييم بالنجوم تفاعلي
- **MapPlaceholder**: عنصر خريطة بديل/تفاعلي

## 4.3 الخادم الخلفي - Node.js + Express.js (Backend)

### 4.3.1 لماذا Node.js و Express.js؟

| الميزة | الشرح |
|---|---|
| **Non-blocking I/O** | يتعامل مع آلاف الطلبات المتزامنة بكفاءة عالية |
| **JavaScript Ecosystem** | نفس اللغة في الفرونت والباك مما يُسهّل التطوير |
| **Express.js** | إطار خفيف وسريع لبناء RESTful APIs |
| **Prisma ORM** | تعامل آمن مع قاعدة البيانات بدون كتابة SQL يدوية |
| **Socket.IO** | دعم مدمج للتواصل الفوري (Real-time) |

### 4.3.2 هيكل الباك إند (Backend Structure)

```
autogo-backend/
├── src/
│   ├── server.js          # نقطة الدخول - إنشاء HTTP Server
│   ├── app.js             # إعداد Express + Middleware + Routes
│   ├── seed.js            # بذر البيانات التجريبية
│   ├── config/
│   │   ├── database.js    # اتصال Prisma بـ PostgreSQL
│   │   ├── redis.js       # اتصال Redis للتخزين المؤقت وOTP
│   │   └── socket.js      # إعداد Socket.IO للتواصل الفوري
│   ├── middleware/
│   │   ├── auth.js        # JWT Authentication Middleware
│   │   ├── errorHandler.js # معالج الأخطاء المركزي
│   │   └── validate.js    # التحقق من صحة البيانات (Zod)
│   └── modules/           # الوحدات البرمجية (9 وحدات)
│       ├── auth/           # المصادقة وتسجيل الدخول
│       ├── users/          # إدارة المستخدمين
│       ├── cars/           # إدارة السيارات
│       ├── services/       # الخدمات المتاحة
│       ├── workshops/      # إدارة الورش
│       ├── orders/         # إدارة الطلبات
│       ├── wallet/         # المحفظة والمعاملات المالية
│       ├── notifications/  # الإشعارات
│       └── addresses/      # العناوين المحفوظة
└── prisma/
    └── schema.prisma       # مخطط قاعدة البيانات (18 جدول)
```

### 4.3.3 الوحدات البرمجية (API Modules)

كل وحدة تتبع نمط **MVC** وتتكون من:
- **Routes** (`.routes.js`): تعريف المسارات (Endpoints)
- **Controller** (`.controller.js`): استقبال الطلبات وإرسال الاستجابات
- **Service** (`.service.js`): المنطق التجاري (Business Logic)
- **Validation** (`.validation.js`): قواعد التحقق باستخدام Zod

| الوحدة | المسار (Endpoint) | الوظائف الرئيسية |
|---|---|---|
| Auth | `/api/auth` | إرسال OTP، التحقق، مزامنة Clerk، تجديد التوكن |
| Users | `/api/users` | عرض وتعديل الملف الشخصي |
| Cars | `/api/cars` | إضافة/تعديل/حذف/عرض السيارات |
| Services | `/api/services` | عرض الخدمات المتاحة والفئات |
| Workshops | `/api/workshops` | عرض الورش مع الفلترة والتقييم |
| Orders | `/api/orders` | إنشاء/تتبع/إلغاء/إكمال الطلبات |
| Wallet | `/api/wallet` | عرض الرصيد والمعاملات |
| Notifications | `/api/notifications` | عرض الإشعارات وتحديثها |
| Addresses | `/api/addresses` | إدارة العناوين المحفوظة |

### 4.3.4 نظام المصادقة والتفويض (Authentication & Authorization)

النظام يدعم **طريقتين** للمصادقة:

**أولاً: Clerk OAuth (الأساسي)**
1. المستخدم يضغط "المتابعة باستخدام جوجل/أبل" في التطبيق
2. Clerk يفتح نافذة مصادقة OAuth آمنة
3. بعد المصادقة الناجحة، يتم مزامنة بيانات المستخدم مع الباك إند عبر `/api/auth/clerk-sync`
4. الباك إند ينشئ/يحدّث المستخدم ويُصدر JWT Tokens

**ثانياً: OTP عبر رقم الهاتف (بديل)**
1. المستخدم يُدخل رقم هاتفه (`+20`)
2. يتم إرسال رمز تحقق (OTP) عبر SMS
3. في بيئة التطوير: يُقبل الرمز `1234` دائماً للاختبار
4. بعد التحقق: يتم إصدار Access Token (7 أيام) و Refresh Token (30 يوم)

**JWT Middleware:**
- كل طلب محمي يجب أن يحتوي على `Authorization: Bearer <token>` في الـ Headers
- الـ Middleware يفك تشفير التوكن ويرفق بيانات المستخدم بالطلب
- في حالة انتهاء الصلاحية، يُرجع كود `TOKEN_EXPIRED` للتطبيق لتجديد التوكن

### 4.3.5 التواصل الفوري - Socket.IO

تم استخدام **Socket.IO** لتوفير اتصال ثنائي الاتجاه في الوقت الحقيقي:

| الحدث (Event) | الاتجاه | الوظيفة |
|---|---|---|
| `join:user` | Client → Server | المستخدم ينضم لغرفته الخاصة |
| `join:driver` | Client → Server | السائق ينضم لغرفته |
| `join:order` | Client → Server | الانضمام لغرفة طلب محدد |
| `driver:location` | Driver → Server → User | تحديث موقع السائق لحظياً |
| `message:send` | Client → Server | إرسال رسالة في المحادثة |
| `message:new` | Server → Client | استلام رسالة جديدة |
| `message:read` | Client → Server → Client | إشعار قراءة الرسالة |

### 4.3.6 Middleware Stack

| الـ Middleware | المكتبة | الوظيفة |
|---|---|---|
| Helmet | `helmet` | حماية HTTP Headers من الهجمات الشائعة |
| CORS | `cors` | التحكم في السماح بالطلبات من مصادر مختلفة |
| Morgan | `morgan` | تسجيل جميع الطلبات الواردة للتتبع |
| JSON Parser | `express.json` | تحليل بيانات JSON في الطلبات (حد 10MB) |
| Error Handler | Custom | معالجة مركزية لكل الأخطاء |
| Validation | `zod` | التحقق من صحة البيانات المدخلة |

---

## 4.4 قاعدة البيانات - PostgreSQL + Prisma ORM

### 4.4.1 لماذا PostgreSQL؟
- **موثوقية عالية**: ACID compliance لضمان سلامة البيانات
- **أداء ممتاز**: يتعامل مع كميات بيانات كبيرة بكفاءة
- **دعم أنواع بيانات متقدمة**: JSON, Arrays, Decimal
- **مجاني ومفتوح المصدر**

### 4.4.2 لماذا Prisma ORM؟
- **Type Safety**: تعريف المخطط بصيغة واضحة ومقروءة
- **Auto-generated Client**: عميل قاعدة البيانات يُنشأ تلقائياً
- **Migrations**: إدارة تطور المخطط بسهولة
- **Prisma Studio**: واجهة مرئية لإدارة البيانات

### 4.4.3 مخطط قاعدة البيانات (Database Schema - 18 جدول)

| الجدول (Table) | الوصف | الحقول الرئيسية |
|---|---|---|
| `users` | بيانات المستخدمين | id, name, phone, email, avatarUrl, city, membershipType, points |
| `cars` | سيارات المستخدمين | id, userId, brand, model, year, plate, color, mileage, status |
| `car_reminders` | تنبيهات السيارات | id, carId, type (license/insurance/service), message, dueDate |
| `addresses` | العناوين المحفوظة | id, userId, label, address, latitude, longitude |
| `service_categories` | فئات الخدمات | id, name, icon, sortOrder |
| `services` | الخدمات المتاحة | id, name, description, basePrice, categoryId |
| `workshops` | ورش الصيانة | id, name, address, lat, lng, rating, openHours, features |
| `workshop_services` | ربط الورش بالخدمات | workshopId, serviceId, customPrice (جدول وسيط M:N) |
| `drivers` | سائقو الونش | id, name, phone, rating, towType, isOnline, currentLat/Lng |
| `orders` | الطلبات | id, orderNumber, userId, carId, driverId, workshopId, type, status, price |
| `order_tracking` | تتبع مراحل الطلب | id, orderId, status, label, timestamp, isCompleted |
| `messages` | رسائل المحادثة | id, orderId, senderType, text, isRead |
| `ratings` | التقييمات | id, orderId, userId, score, tags, comment |
| `wallets` | محافظ المستخدمين | id, userId, balance (EGP), currency |
| `transactions` | المعاملات المالية | id, walletId, type (deposit/payment/refund), amount |
| `payment_methods` | طرق الدفع | id, userId, type (meeza/vodafone_cash/fawry/visa), lastFour |
| `notifications` | الإشعارات | id, userId, title, body, type (urgent/success/promo/reminder) |
| `invoices` | الفواتير الرقمية | id, orderId, invoiceNo, subtotal, discount, tax, total |
| `promo_codes` | أكواد الخصم | id, code, discountType, discountValue, maxUses, validUntil |

### 4.4.4 العلاقات بين الجداول (Relationships)

- **User** → Cars (1:M), Addresses (1:M), Orders (1:M), Wallet (1:1), PaymentMethods (1:M), Ratings (1:M), Notifications (1:M)
- **Car** → CarReminders (1:M), Orders (1:M)
- **Workshop** → WorkshopServices (M:N with Service), Orders (1:M), Ratings (1:M)
- **Order** → OrderTracking (1:M), Messages (1:M), Rating (1:1), Invoice (1:1)
- **Wallet** → Transactions (1:M)

### 4.4.5 مسار العمل النموذجي (Example Workflow)

**مثال: طلب خدمة ونش**
1. التطبيق يرسل `POST /api/orders` مع بيانات الطلب (نوع الونش، الموقع، السيارة)
2. الباك إند يتحقق من التوكن عبر JWT Middleware
3. الـ Service Layer يتحقق من صحة البيانات ويُنشئ الطلب في PostgreSQL
4. يتم إنشاء سجلات `order_tracking` للمراحل المختلفة
5. Socket.IO يبث إشعاراً للسائقين المتاحين عبر `emitToUser`
6. عند قبول السائق، يتم تحديث حالة الطلب ويبدأ البث المباشر لموقعه
7. يتم إرجاع `JSON Response` للتطبيق مع تفاصيل السائق والـ ETA

---

## 4.5 الأمان والأداء (Security & Performance)

### 4.5.1 إجراءات الأمان

| الإجراء | التقنية | الشرح |
|---|---|---|
| **تشفير الاتصال** | HTTPS | جميع البيانات مشفرة أثناء النقل |
| **مصادقة آمنة** | JWT + Clerk | توكنات مؤقتة مع مزود هوية خارجي |
| **حماية الـ Headers** | Helmet.js | منع هجمات XSS, Clickjacking وغيرها |
| **تخزين آمن** | expo-secure-store | تخزين التوكنات في مساحة مشفرة على الجهاز |
| **تحقق من البيانات** | Zod | رفض أي بيانات غير مطابقة للمخطط |
| **CORS** | cors middleware | تقييد المصادر المسموح لها بالوصول |
| **تشفير كلمات المرور** | bcryptjs | في حالة استخدام OTP مع تخزين مؤقت |
| **حماية من الحقن** | Prisma ORM | Parameterized Queries تلقائياً |

### 4.5.2 تحسينات الأداء

| التحسين | الشرح |
|---|---|
| **Redis Caching** | تخزين OTP والبيانات المتكررة في الذاكرة |
| **Async/Await** | معالجة غير متزامنة لكل العمليات |
| **Connection Pooling** | إعادة استخدام اتصالات قاعدة البيانات |
| **JSON Limit** | تحديد حجم الطلبات (10MB) لمنع الإغراق |
| **Indexed Fields** | فهرسة الحقول المستخدمة في البحث (email, phone, orderNumber) |
| **Lazy Loading** | تحميل الشاشات عند الحاجة فقط |

---

## 4.6 الاختبار والتحقق (Testing & Validation)

| نوع الاختبار | الوصف | النتيجة |
|---|---|---|
| **Functional Testing** | اختبار الوظائف الأساسية: تسجيل الدخول (OAuth)، إضافة سيارة، طلب ونش، حجز صيانة، التتبع المباشر | ✅ Passed |
| **UI/UX Testing** | التحقق من العرض الصحيح على أجهزة Android مختلفة، اتجاه RTL، الوضع الداكن | ✅ Passed |
| **API Testing** | اختبار جميع الـ 9 وحدات API (CRUD operations) باستخدام أدوات مثل Postman | ✅ Passed |
| **Real-time Testing** | اختبار Socket.IO: تحديث الموقع، المحادثة، الإشعارات الفورية | ✅ Passed |
| **Performance Testing** | قياس زمن الاستجابة تحت ضغط متعدد الطلبات | 🟡 Stable |
| **Security Testing** | التحقق من حماية JWT، عزل البيانات بين المستخدمين، حماية الـ Endpoints | 🔒 Secure |
| **Database Testing** | التحقق من العلاقات، القيود (Constraints)، التكامل المرجعي | ✅ Passed |
| **Cross-Platform Testing** | اختبار على Android (Physical + Emulator) | ✅ Passed |

---

## 4.7 التقنيات والمكتبات المستخدمة (Technology Stack Summary)

### Frontend (Mobile)
| المكتبة | الإصدار | الاستخدام |
|---|---|---|
| React Native | 0.81.5 | إطار عمل التطبيق الأساسي |
| Expo | 54.0 | أدوات التطوير والحزم الأصلية |
| Redux Toolkit | 2.11 | إدارة الحالة المركزية |
| React Navigation | 7.x | التنقل بين الشاشات |
| Clerk | 2.19 | مصادقة OAuth (Google/Apple) |
| react-native-maps | 1.20 | الخرائط التفاعلية |
| expo-location | 19.0 | خدمات الموقع الجغرافي |
| expo-secure-store | 15.0 | التخزين المشفر للتوكنات |
| expo-linear-gradient | 15.0 | التدرجات اللونية |
| TypeScript | 5.9 | الكتابة الآمنة للأنواع |

### Backend
| المكتبة | الإصدار | الاستخدام |
|---|---|---|
| Express.js | 5.2 | إطار عمل الخادم |
| Prisma ORM | 7.7 | التعامل مع قاعدة البيانات |
| Socket.IO | 4.8 | التواصل الفوري |
| jsonwebtoken | 9.0 | إصدار والتحقق من JWT |
| bcryptjs | 3.0 | تشفير البيانات الحساسة |
| Helmet | 8.1 | حماية HTTP Headers |
| Zod | 4.3 | التحقق من صحة البيانات |
| ioredis | 5.10 | التواصل مع Redis |
| Morgan | 1.10 | تسجيل الطلبات |

---

## 4.8 الخلاصة (Conclusion)

> نجح نظام **AutoGo** في تطبيق معمارية ثلاثية الطبقات متكاملة ومتماسكة:
>
> - **React Native + Expo** وفّر تطبيق موبايل عالي الأداء يعمل على Android و iOS بكود واحد، مع واجهة مستخدم عصرية داكنة وتجربة مستخدم سلسة تضم أكثر من 41 شاشة.
>
> - **Node.js + Express.js** قدّم خادماً خلفياً سريعاً وآمناً يدعم 9 وحدات API مستقلة، مع مصادقة JWT/Clerk ومعالجة متزامنة كفء.
>
> - **PostgreSQL + Prisma** ضمنا قاعدة بيانات قوية ومُنظّمة بـ 18 جدولاً يغطي كل كيانات النظام مع علاقات واضحة وقيود تكاملية.
>
> - **Socket.IO + Redis** أضافا بُعداً فورياً للنظام عبر البث المباشر لمواقع السائقين والمحادثات اللحظية.
>
> النظام جاهز للتوسع المستقبلي ليشمل لوحة إدارة (Admin Dashboard)، تطبيق مخصص للسائقين، وتكامل مع بوابات دفع حقيقية مثل Paymob و Fawry.
